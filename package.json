{
  "name": "document-saas",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"**/*.{js,json,md,ts,tsx,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{js,json,md,ts,tsx,yaml,yml}\"",
    "infra:up": "docker compose -f infra/docker-compose.yml up -d",
    "infra:down": "docker compose -f infra/docker-compose.yml down",
    "infra:logs": "docker compose -f infra/docker-compose.yml logs -f",
    "infra:ps": "docker compose -f infra/docker-compose.yml ps",
    "db:generate": "pnpm --filter @document-saas/db db:generate",
    "db:migrate": "pnpm --filter @document-saas/db db:migrate",
    "db:studio": "pnpm --filter @document-saas/db db:studio",
    "db:check": "pnpm --filter @document-saas/db db:check",
    "db:smoke-rls": "pnpm --filter @document-saas/db db:smoke-rls"
  },
  "devDependencies": {
    "prettier": "^3.9.5",
    "turbo": "^2.10.5",
    "typescript": "catalog:"
  }
}
